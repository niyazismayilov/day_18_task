import 'package:flutter/material.dart';


class Pager {
  Pager._();

}

// Go.to(Pager.splash); //! Bu cur navigate edilecek (push)
// Go.replace(Pager.otp); //! Bu cur navigate edilecek (push replace)
